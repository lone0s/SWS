{% block bienvenue %}
    <div class="title">
        <img src="{{ asset('img/logo.png') }}" alt="logo"/>
        {% if app.user == null %}
            <h1>Bonjour</h1>
        {% else %}
            <h1>Bonjour {{ app.user.login }}</h1>
        {% endif %}
    </div>
{% endblock %}
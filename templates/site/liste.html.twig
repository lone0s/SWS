<h2>Liste de tous les articles</h2>
{#{{ dump(articles) }}#}
    <table>
        <tr>
            <th>
                id
            </th>
            <th>
                libelle
            </th>
            <th>
                prix
            </th>
            <th>
                quantité
            </th>
        </tr>
        {% for article in articles %}
            <tr>
                <td>
                    {{ article.id }}
                </td>
                <td>
                    {{ article.libelle }}
                </td>
                <td>
                    {{ article.price }}
                </td>
                <td>
                    {{ article.quantity }}
                </td>
            </tr>
        {% endfor %}
    </table>